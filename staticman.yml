comments:
  allowedFields  : ['name', 'email', 'comment', 'pic', 'parent', 'timestamp']
  branch         : master
  commitMessage  : "New comment on post with pic {fields.pic}"
  filename       : "{fields.pic}{fields.timestamp}"
  format         : yml
  moderation     : true
  path           : "_data/comments"
  requiredFields : ['name', 'comment', 'pic', 'parent', 'timestamp']

  transforms:
    email: md5
